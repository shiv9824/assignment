#include <stdio.h>
#include <conio.h>

int main()
{
    float a;
    printf("\nEnter the side of the Square: ");
    scanf("%f",&a);
    a=a*a;

    printf("\nArea of the Square = %f\n",a);
    return 0;
}